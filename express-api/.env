# Express API environment configuration
# Fill MONGO_URI with your MongoDB Atlas connection string

# Server
PORT=3000

# MongoDB Atlas URI (example format below). Replace <username>, <password>, <cluster-host>, and optional appName.
# You can get this from Atlas: Database > Connect > Drivers > Node.js > Copy connection string
MONGO_URI=mongodb+srv://Oliveth:Billionaire1.@cluster0.5lphwdr.mongodb.net/?appName=Cluster0
JWT_ACCESS_SECRET=change_me_in_production
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL_DAYS=7
REFRESH_COOKIE_NAME=refresh_token

# CORS (comma-separated list). Keep localhost for Vite dev server.
CORS_ORIGINS=http://localhost:5173
